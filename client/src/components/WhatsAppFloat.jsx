import {MessageCircle} from 'lucide-react';import {useSiteConfig} from '../hooks/useSiteConfig';
export default function WhatsAppFloat(){const {data:cfg}=useSiteConfig();const s=cfg?.settings||{};if(s['whatsapp.enabled']==='0'||!s['whatsapp.number'])return null;const number=String(s['whatsapp.number']).replace(/\D/g,''),text=encodeURIComponent(s['whatsapp.message']||'Hello Medico, I need help.');return <a href={`https://wa.me/${number}?text=${text}`} target="_blank" rel="noreferrer" aria-label="Chat with Medico on WhatsApp" className="fixed bottom-5 right-5 z-50 grid h-14 w-14 place-items-center rounded-full bg-emerald-500 text-white shadow-xl transition hover:scale-105"><MessageCircle size={28}/></a>}
