import {useState} from 'react';import {useNavigate} from 'react-router-dom';import http from '../api/http';import {useAuthStore} from '../store/auth';import {AuthShell} from './Login';export default function Register(){const [f,setF]=useState({firstName:'',lastName:'',email:'',phone:'',password:''});const setSession=useAuthStore(s=>s.setSession);const nav=useNavigate();const submit=async e=>{e.preventDefault();try{const r=await http.post('/auth/register',f);setSession(r.data.data);nav('/')}catch(e){alert(e.response?.data?.message||'Registration failed')}};return <AuthShell title="Create account"><form className="space-y-3" onSubmit={submit}>{[['firstName','First name'],['lastName','Last name'],['email','Email'],['phone','Phone'],['password','Password']].map(([k,l])=><input key={k} type={k==='password'?'password':'text'} className="input" placeholder={l} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<button className="btn-primary w-full">Create Account</button></form></AuthShell>}