import {useQuery} from '@tanstack/react-query';import {Link} from 'react-router-dom';import {FileText} from 'lucide-react';import http from '../../api/http';export default function Orders(){const {data=[]}=useQuery({queryKey:['orders'],queryFn:()=>http.get('/user/orders').then(r=>r.data.data)});return <><h1 className="text-3xl font-black">My Orders</h1><div className="mt-6 space-y-3">{data.map(o=><div className="card flex flex-wrap items-center justify-between gap-3 p-5" key={o.id}><div><div className="font-black">{o.order_number}</div><div className="text-sm text-slate-500">{new Date(o.created_at).toLocaleString()}</div></div><div className="badge bg-brand-50 text-brand-700 capitalize">{String(o.status).replaceAll('_',' ')}</div><div className="font-black">৳{Number(o.total).toLocaleString()}</div><Link to={'/invoice/'+o.id} className="btn-secondary px-3 py-2 text-sm"><FileText size={16}/>Invoice</Link></div>)}{!data.length&&<div className="card p-10 text-center">No orders yet.</div>}</div></>}