import {useQuery} from '@tanstack/react-query';import http from '../../api/http';import ProductCard from '../../components/ProductCard';export default function Wishlist(){const {data=[]}=useQuery({queryKey:['wishlist'],queryFn:()=>http.get('/user/wishlist').then(r=>r.data.data)});return <><h1 className="text-3xl font-black">Wishlist</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{data.map(p=><ProductCard p={p} key={p.id}/>)}</div></>}