import {useState} from 'react';
import {useQuery,useQueryClient} from '@tanstack/react-query';
import {Link} from 'react-router-dom';
import {Plus,Search,Pencil,Trash2,Braces,Package} from 'lucide-react';
import http,{assetUrl} from '../../api/http';

export default function AdminProducts(){
  const qc=useQueryClient();const [q,setQ]=useState('');const [status,setStatus]=useState('');const {data,isLoading}=useQuery({queryKey:['adminProducts',q,status],queryFn:()=>http.get('/admin/products',{params:{q,status,limit:100}}).then(r=>r.data)});const rows=data?.data||[];
  const remove=async p=>{if(!window.confirm(`Archive ${p.name}? Existing order history will remain intact.`))return;try{await http.delete('/admin/products/'+p.id);qc.invalidateQueries({queryKey:['adminProducts']})}catch(e){alert(e.response?.data?.message||'Could not archive product')}};
  return <><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 className="admin-page-title">Products</h1><p className="mt-1 text-sm text-slate-500">Full catalog control including rich descriptions, price, visibility, cover and gallery images, stock and JSON import.</p></div><div className="flex flex-wrap gap-2"><Link className="btn-secondary" to="/admin/products/new"><Braces size={17}/>JSON Import</Link><Link className="btn-primary" to="/admin/products/new"><Plus size={18}/>Add Product</Link></div></div><div className="admin-section mt-6 p-0 overflow-hidden"><div className="flex flex-col gap-3 border-b p-4 sm:flex-row"><label className="flex flex-1 items-center gap-2 rounded-xl border border-slate-200 px-3"><Search size={17} className="text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} className="h-11 flex-1 text-sm" placeholder="Search name, SKU, slug or generic name..."/></label><select className="input sm:w-44" value={status} onChange={e=>setStatus(e.target.value)}><option value="">All status</option><option value="active">Active</option><option value="inactive">Inactive</option></select></div><div className="overflow-x-auto"><table className="admin-table"><thead><tr><th>Product</th><th>SKU</th><th>Category</th><th>Price</th><th>Stock</th><th>Flags</th><th>Status</th><th className="text-right">Actions</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td><div className="flex min-w-64 items-center gap-3">{p.image?<img src={assetUrl(p.image)} className="h-12 w-12 rounded-xl bg-slate-50 object-contain"/>:<div className="grid h-12 w-12 place-items-center rounded-xl bg-slate-100"><Package size={18}/></div>}<div><div className="font-bold">{p.name}</div><div className="text-xs text-slate-500">{p.generic_name||p.slug}</div></div></div></td><td className="font-mono text-xs">{p.sku}</td><td>{p.category||'—'}</td><td><b>৳{Number(p.sale_price).toLocaleString()}</b>{Number(p.regular_price)>Number(p.sale_price)&&<div className="text-xs text-slate-400 line-through">৳{Number(p.regular_price).toLocaleString()}</div>}</td><td><span className={`badge ${Number(p.stock)<=0?'bg-rose-50 text-rose-700':Number(p.stock)<=10?'bg-amber-50 text-amber-700':'bg-emerald-50 text-emerald-700'}`}>{p.stock}</span></td><td><div className="flex flex-wrap gap-1">{p.prescription_required?<span className="badge bg-violet-50 text-violet-700">Rx</span>:null}{p.is_featured?<span className="badge bg-sky-50 text-sky-700">Featured</span>:null}{p.is_best_seller?<span className="badge bg-amber-50 text-amber-700">Best</span>:null}</div></td><td><span className={`badge ${p.is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}`}>{p.is_active?'Active':'Inactive'}</span></td><td><div className="flex justify-end gap-1"><Link to={`/admin/products/${p.id}/edit`} className="btn-ghost"><Pencil size={16}/>Edit</Link><button onClick={()=>remove(p)} className="btn-ghost text-rose-600"><Trash2 size={16}/></button></div></td></tr>)}{!rows.length&&!isLoading&&<tr><td colSpan="8" className="py-12 text-center text-slate-400">No products found.</td></tr>}</tbody></table></div></div></>;
}
