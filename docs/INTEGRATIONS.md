# Integration settings

The current complete setup guide is [INTEGRATION_SETTINGS_BN.md](../INTEGRATION_SETTINGS_BN.md). It covers cPanel, SMTP/IMAP, encryption setup, SMS, payments, notifications, analytics, quantity offers and product images.

Mail credentials are configured only in Admin Email Settings; SMTP_/IMAP_/MAIL_FROM_ environment values are no longer used. Migration 004 follows the existing migrations.
