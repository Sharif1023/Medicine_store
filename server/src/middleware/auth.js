import {verifyAccess} from '../utils/tokens.js'; import {query} from '../config/db.js'; export async function auth(req,res,next){try{const h=req.headers.authorization||''; if(!h.startsWith('Bearer ')) return res.status(401).json({success:false,message:'Authentication required'}); const p=verifyAccess(h.slice(7)); const rows=await query('SELECT id,email,first_name,last_name,phone,is_active FROM users WHERE id=? AND deleted_at IS NULL',[p.sub]); if(!rows[0]||!rows[0].is_active) return res.status(401).json({success:false,message:'Account unavailable'}); req.user=rows[0]; next();}catch(e){return res.status(401).json({success:false,message:'Invalid or expired access token'});}} export function optionalAuth(req,res,next){if(!req.headers.authorization)return next();return auth(req,res,()=>next());}