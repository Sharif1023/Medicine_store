import {query,pool} from '../config/db.js';
try{const [s]=await query('SELECT retention_days FROM analytics_settings WHERE id=1');const days=Math.max(1,Math.min(365,Number(s?.retention_days)||90));await query('DELETE FROM user_activity_logs WHERE created_at<DATE_SUB(NOW(),INTERVAL ? DAY)',[days]);await query("DELETE FROM notification_outbox WHERE created_at<DATE_SUB(NOW(),INTERVAL 30 DAY) AND status IN ('sent','failed','skipped')");await query('DELETE FROM password_reset_tokens WHERE expires_at<NOW()');await query('DELETE FROM verification_otps WHERE expires_at<NOW()');console.log('Retention cleanup complete')}finally{await pool.end()}
